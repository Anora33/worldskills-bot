name: Keep Bot Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Har 5 daqiqada
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot
        run: curl -s -o /dev/null -w "%{http_code}" https://worldskills-bot.onrender.com/health
